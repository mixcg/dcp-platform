zuul:
  routes:
    bams:
      path: /security/**
      serviceId: dunzung-cloud-security
      sensitiveHeaders:
    sso:
      path: /sso/**
      serviceId: dunzung-cloud-ssoserver
      sensitiveHeaders:
  ignored-services: "*"

#################################################################
security:
  oauth2:
    client:
      client-id: jund
      client-secret: secret
      access-token-uri: http://dunzung-cloud-ssoserver/sso/oauth/token
      user-authorization-uri: http://dunzung-cloud-ssoserver/sso/oauth/authorize
    resource:
      user-info-uri: http://dunzung-cloud-ssoserver/sso/api/v1/sso/authinfo
      prefer-token-info: false

management:
  security:
    enabled: false